[package]
name = "tack"
version = "0.7.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.13", features = ["blocking"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
libc = "0.2"
